# Gradle
.gradle/
build/
.kotlin/
local.properties

# Signing: keystore/ and keystore.properties ARE committed on purpose (private repo, archived
# with the code). Never make this repository public without rotating the upload key.

# Release outputs
dist/

# IDE
.idea/
*.iml
.DS_Store
